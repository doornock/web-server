Doornock
=============

Instalation:
1) Directory log and temp should be writeable (chmod 0777)
2) app/config/config.local.neon.example copy as app/config/config.local.neon and modify configuration
3) run: composer install

